# Agent 32 — V1 API — Backend Developer Review

**Total Issues Found: 19**
- CRITICAL: 3 / HIGH: 5 / MEDIUM: 7 / LOW: 4

---

## CRITICAL

| ID | File:Line | Description | Evidence |
|----|-----------|-------------|----------|
| C1 | `src/lib/rate-limit.ts:74-82` | **In-memory rate limiter never records hits — rate limiting is completely non-functional.** `rateLimit()` calls `getRateLimitStatus()` which reads timestamps from the `store` Map, but neither function ever pushes a new timestamp into the Map. Every call returns `remaining = limit` (full quota). All V1 routes use this synchronous `rateLimit()` via `validateV1Auth()`, meaning no API consumer is ever actually rate-limited. | `rateLimit()` at line 74 only reads `remaining` via `getRateLimitStatus()` and returns — no `store.get(id).push(Date.now())` or equivalent write. The `cleanup()` function (line 33) is also never called. |
| C2 | `src/app/api/v1/chat/batch/route.ts:89-99` | **Batch processing blocks the HTTP response — client receives response only after all requests complete.** `await processBatch(...)` is called before returning the `apiSuccess` response on line 92. For 50 requests with 60s timeouts each (processed 3 at a time), this could block for up to ~17 minutes. The `maxDuration` is only 300s (5min), so long batches will be killed by the serverless runtime mid-execution, leaving the batch in a permanent "processing" state with no cleanup. | Line 90: `await processBatch(admin, batchId, ...)` — then line 92: `return apiSuccess(...)`. The response body claims `status: "processing"` and `completed: 0`, but by the time the client receives it, processing is already done (or the function timed out). |
| C3 | `src/app/api/v1/files/route.ts:127-134` | **File upload continues and returns success even when storage upload fails.** If Supabase Storage upload fails (line 131), the code only logs a warning and proceeds to insert a `kb_documents` record pointing to a non-existent storage path. Subsequent reads or indexing of binary files (PDF, images) will silently fail. | Lines 131-134: `if (uploadError) { console.warn(...); }` — execution continues, inserts DB record at line 146 with the `storage_path` that was never written. |

## HIGH

| ID | File:Line | Description | Evidence |
|----|-----------|-------------|----------|
| H1 | `src/app/api/v1/usage/route.ts:16-19` | **Unbounded query on `agent_analytics` — no row limit.** Fetches all analytics rows for a key over up to 90 days with no `.limit()`. A high-traffic key could have hundreds of thousands of rows, causing memory exhaustion and slow response times. The client-side aggregation at lines 26-33 then iterates every row in JS. | Line 16-19: `.select("metric_type, response_time_ms, created_at").eq("api_key_id", apiKey.id).gte("created_at", since)` — no `.limit()` or server-side aggregation. |
| H2 | `src/app/api/v1/chat/route.ts:117-124` | **Default agent selection is non-deterministic.** When no `agent` is specified, the query fetches the "first" deployed agent with `.limit(1).single()` but has no `.order()` clause. The returned agent depends on database internal ordering and can change between requests, giving inconsistent behavior. | Lines 118-124: `.eq("deployed", true).limit(1).single()` with no `order()`. |
| H3 | `src/app/api/v1/threads/[id]/messages/route.ts:86-93` | **User message insert ignores error — silent data loss.** The `await admin.from("api_thread_messages").insert(...)` at line 88 does not check for errors. If the insert fails (e.g., constraint violation, DB outage), the route continues to call the agent and returns a success response, but the user's message is lost from the thread history. | Lines 88-93: `await admin.from("api_thread_messages").insert({...});` — no `.then()` error check, no `const { error }` destructuring. Same issue for the assistant message insert at line 162. |
| H4 | `src/app/api/v1/threads/[id]/route.ts:41` | **Thread deletion does not explicitly delete child messages.** The DELETE handler at line 41 deletes only from `api_threads`. If the database does not have `ON DELETE CASCADE` on `api_thread_messages.thread_id`, orphaned messages will remain. No explicit cleanup of `api_thread_messages` is performed in the code. | Line 41: `admin.from("api_threads").delete().eq("id", id)...` — no corresponding delete on `api_thread_messages`. |
| H5 | `src/app/api/v1/files/[id]/route.ts:50-64` | **TOCTOU race in file deletion.** Two separate queries: first SELECT the file to get `storage_path` (line 50-52), then DELETE the DB record (line 61-62). Between these two operations, another concurrent request could delete the same file, causing the second DELETE to return no rows (returning "File not found" to one caller), while storage was already cleaned up by the first. The operations should be combined or use a transaction. | Lines 50-62: separate `.select()` then `.delete()` without a transaction. |

## MEDIUM

| ID | File:Line | Description | Evidence |
|----|-----------|-------------|----------|
| M1 | `src/app/api/v1/chat/route.ts:227` | **Timeout not cleared on abort.** `clearTimeout(timeout)` on line 227 is reached only if `fetch` succeeds. If `controller.abort()` fires first (line 181), the `fetch` throws, and `clearTimeout` is never reached — the timeout callback fires again against an already-aborted controller. Should use try/finally. | Lines 180-227: `const timeout = setTimeout(...)` then `clearTimeout(timeout)` after the fetch, but the fetch can throw `AbortError` before reaching the clear. |
| M2 | `src/app/api/v1/predictions/route.ts:23-32` | **Predictions are created but never processed.** The POST handler inserts a prediction with `status: "pending"` but never starts any background processing. There is no worker, cron job, or queue consumer referenced anywhere. The prediction will remain "pending" forever. | Lines 23-30: `insert({ status: "pending", ... })` with no follow-up processing. No background task or webhook trigger. |
| M3 | `src/app/api/v1/audit-log/route.ts:34-35` | **Unsanitized `from`/`to` date parameters passed directly to query.** The `from` and `to` query params are used directly in `.gte("created_at", from)` and `.lte("created_at", to)` without validating they are valid ISO date strings. Malformed values will cause Supabase/Postgres errors that bubble up as generic "Failed to fetch audit logs". | Lines 34-35: `if (from) query = query.gte("created_at", from);` — no date format validation. |
| M4 | `src/app/api/v1/threads/route.ts:50-51` | **Thread POST does not validate body parse failure.** `request.json().catch(() => null)` returns `null` on invalid JSON, and then line 51 reads `(body as any)?.agent || "default"`. This silently creates a thread with defaults instead of returning a 400 error for malformed input. | Line 50: `const body = await request.json().catch(() => null);` — null body is not rejected. |
| M5 | `src/app/api/v1/chat/batch/route.ts:123` | **Batch fails silently when dashboardUrl is null.** In `processBatch()`, if `dashboardUrl` is null (line 123), the function just returns without updating the batch status. The batch remains stuck in "processing" status forever with no error indication to the user. | Line 123: `if (!dashboardUrl) return;` — no `admin.from("api_batches").update({ status: "failed" })`. Compare with the VPS not-running check on line 118 which does update to "failed". |
| M6 | `src/lib/v1-auth.ts:59` | **Hardcoded API key length check is fragile.** Line 59 rejects keys where `rawKey.length !== 36`. If the key generation format ever changes (e.g., longer random segment), all new keys will be rejected at auth. This should validate a minimum length and prefix rather than an exact length. | Line 59: `rawKey.length !== 36` — brittle exact-length check. |
| M7 | `src/app/api/v1/agents/[id]/route.ts:75-77` | **SSH undeploy failure is silently swallowed.** The DELETE handler catches all SSH errors (line 75) and proceeds to mark the agent as undeployed in the DB. If the SSH operation fails, the agent process remains running on the VPS but the DB says it's undeployed, causing a state mismatch. | Lines 75-77: `catch { // SSH failure — still mark as undeployed in DB }` |

## LOW

| ID | File:Line | Description | Evidence |
|----|-----------|-------------|----------|
| L1 | `src/app/api/v1/conversations/route.ts:5` | **Docstring says "cursor-based pagination" but implementation is offset-based.** The comment on line 5 says "cursor-based pagination" but the actual implementation uses `offset`/`limit` query parameters (lines 15-18). This is misleading for anyone maintaining or consuming the API. | Line 5: `/** GET /api/v1/conversations — List user's conversations (cursor-based pagination) */` vs actual `offset` param at line 17. Same issue in `threads/[id]/messages/route.ts` line 8. |
| L2 | `src/app/api/v1/chat/route.ts:96-101` | **Agent lookup fetches all deployed agents into memory.** When an `agent` name is specified, the query fetches ALL deployed agents for the user (no server-side name filter), then does a `.find()` in JavaScript. For users with many agents this is wasteful. A server-side `.ilike()` filter would be more efficient. | Lines 96-100: `.select(...).eq("user_id", userId).eq("deployed", true)` with no name filter, followed by JS `.find()` on line 102. |
| L3 | `src/app/api/v1/health/route.ts:13-17` | **Health endpoint fetches all deployed agents without pagination.** The query at line 13 selects all deployed agents with no `.limit()`. While unlikely to be large, it violates the pattern of bounded queries used elsewhere. | Lines 13-17: `.select("agents(name)").eq("user_id", apiKey.user_id).eq("deployed", true)` — no limit. |
| L4 | `src/app/api/v1/files/route.ts:152` | **Redundant ternary — both branches are identical.** `status: textContent ? "processing" : "processing"` always evaluates to `"processing"`. This is dead logic, suggesting the author intended different statuses for text vs binary files. | Line 152: `status: textContent ? "processing" : "processing"` |
