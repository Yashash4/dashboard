# Agent 07 — Pro Tier — Backend Developer Review

**Total Issues Found: 32**
- CRITICAL: 4
- HIGH: 10
- MEDIUM: 12
- LOW: 6

---

## CRITICAL

| ID | File:Line | Description | Evidence |
|----|-----------|-------------|----------|
| PRO_B_CRIT_01 | `src/lib/rate-limit.ts:74-82` | In-memory rate limiter never actually records hits — `rateLimit()` calls `getRateLimitStatus()` which reads timestamps but never pushes to the store Map. The `store` Map is never written to, so rate limiting is completely non-functional. Every call returns `remaining = limit`, meaning all rate limits across every route are bypassed. | `getRateLimitStatus` filters `store.get(identifier)` but no code ever calls `store.set()` or pushes timestamps. The old `rateLimit` function was rewritten to delegate to `getRateLimitStatus` without the `store.get(identifier)...push(now)` logic. |
| PRO_B_CRIT_02 | `src/app/api/v1/chat/batch/route.ts:90` | Batch processing is `await`ed inside the POST handler, meaning the HTTP response at line 92-99 is only sent AFTER all batch items complete (up to 50 items x 60s timeout each = potentially 1000s). The response claims `status: "processing"` and `completed: 0` but by the time it's returned, the batch is already fully processed or failed. Clients get a misleading response and extremely long request times. | Line 90: `await processBatch(admin, batchId, ...)` blocks, then line 92-99 returns `status: "processing", completed: 0` — but the batch is already done. |
| PRO_B_CRIT_03 | `src/app/api/mission-control/tasks/bulk-action/route.ts:29` | Bulk delete uses hard `DELETE` (`supabase.from("mc_tasks").delete()`) while the single task DELETE at `tasks/[id]/route.ts:257-262` uses soft delete (`update({ deleted_at })`). This data integrity mismatch means bulk-deleted tasks are permanently destroyed, bypassing the soft-delete audit trail and making them unrecoverable. | Line 29: `.delete().eq("user_id", user.id).in("id", task_ids)` vs `tasks/[id]/route.ts:259`: `.update({ deleted_at: new Date().toISOString() })` |
| PRO_B_CRIT_04 | `src/app/api/mission-control/tasks/bulk-action/route.ts:31-39` | Bulk "move" action accepts any `value` string as `column_id` without validation. An attacker can set `column_id` to arbitrary values (e.g., SQL-injection-like strings or invalid states), corrupting the task state machine. The single-task PATCH validates against `VALID_COLUMNS`, but bulk-action does not. Same issue for bulk "priority" at line 44 — no validation against `VALID_PRIORITIES`. | Line 33: `column_id: value` where `value` comes directly from user input with no validation. Compare to `tasks/[id]/route.ts:95-101` which validates against `VALID_COLUMNS`. |

## HIGH

| ID | File:Line | Description | Evidence |
|----|-----------|-------------|----------|
| PRO_B_HIGH_01 | `src/app/api/v1/files/route.ts:161` | `indexDocument()` is called with `.catch(() => {})` (fire-and-forget), but the API immediately returns `status: "processing"`. If indexing throws, the document stays in `processing` status forever — there is no error handler to set it to `failed`. The `markStaleProcessingFiles` function only runs on GET list requests and uses a 5-minute threshold, so files could appear stuck for up to 5 minutes until the next GET. | Line 161: `indexDocument(fileId, apiKey.user_id, textContent, fileType).catch(() => {});` — the empty catch swallows all errors including DB failures. |
| PRO_B_HIGH_02 | `src/app/api/v1/chat/route.ts:231-241` | Analytics insert on upstream error is fire-and-forget with no await. On serverless platforms (Vercel), the function may terminate before the DB write completes, losing error tracking data. Multiple places in this file use the same pattern (lines 308-320, 367-381). | Lines 231-241, 308-320, 367-381: `admin.from("agent_analytics").insert({...}).then(() => {}, ...)` — non-awaited promise in a handler that's about to return. |
| PRO_B_HIGH_03 | `src/app/api/mission-control/tasks/[id]/dependencies/route.ts:60-81` | Circular dependency check performs N+1 queries — one DB query per node in the dependency graph traversal. With deep or wide dependency chains, this causes O(n) sequential database round-trips. A malicious user could create a long chain to cause slow responses. | Lines 70-80: `while (stack.length > 0) { ... const { data: transitive } = await supabase.from("mc_task_dependencies").select(...)` — each iteration makes a DB call. |
| PRO_B_HIGH_04 | `src/app/api/analytics/funnels/route.ts:38-47` | Fetches ALL messages for up to 200 conversations with `select("conversation_id")` but no `.limit()`. For active users, this could return tens of thousands of rows just to count messages per conversation. Should use a grouped count RPC or `select("id", { count: "exact", head: true })` per conversation. | Line 39-42: `admin.from("chat_messages").select("conversation_id").in("conversation_id", convIds)` — unbounded row fetch. |
| PRO_B_HIGH_05 | `src/app/api/keys/route.ts:55-72` | Key usage stats fetch ALL `agent_analytics` rows for the week to count them client-side. For active API keys, this could be thousands of rows per key. Three separate unbounded queries (today, week, errors) each fetch full rows just to count them. Should use `{ count: "exact", head: true }` or a DB function. | Lines 55-72: Three unbounded `select("api_key_id")` queries without `.limit()` or `count: "exact"`. |
| PRO_B_HIGH_06 | `src/app/api/v1/usage/route.ts:16-19` | Fetches ALL analytics rows for the date range (up to 90 days) into memory to compute aggregates client-side. For high-traffic API keys, this could be hundreds of thousands of rows. Should use a DB aggregate query. | Line 16-19: `admin.from("agent_analytics").select("metric_type, response_time_ms, created_at").eq("api_key_id", apiKey.id).gte("created_at", since)` — no limit, fetches all rows. |
| PRO_B_HIGH_07 | `src/app/api/mission-control/tasks/reorder/route.ts:75-86` | Reorder fires up to 100 concurrent UPDATE queries via `Promise.all()`. This can overwhelm the database connection pool and cause timeout errors. Same issue in `bulk-update/route.ts:27-38`. | Line 75-88: `Promise.all(updates.map((u) => supabase.from("mc_tasks").update(...)))` with up to 100 items. |
| PRO_B_HIGH_08 | `src/app/api/mission-control/tasks/bulk-update/route.ts:13-23` | `bulk-update` route has no input validation on `column_id` or `position` values, and no ownership verification that all task IDs belong to the user before updating. Compare with `tasks/reorder/route.ts` which validates UUID format, column_id against whitelist, position range, and verifies ownership. | Line 27-38: Updates tasks without validating `column_id` is valid or that `position` is a number. |
| PRO_B_HIGH_09 | `src/app/api/v1/predictions/route.ts:7-44` | Predictions are created with `status: "pending"` but there is no background worker or processing pipeline to actually execute them. The prediction stays in `pending` forever — it's a dead-end API that creates orphaned records. | Line 23-30: Inserts prediction with `status: "pending"` and `response_body: null`, but no code ever processes it or updates the status to `completed`. |
| PRO_B_HIGH_10 | `src/app/api/cron/apply-pending-changes/route.ts:143-148` | Error messages from SSH operations and exceptions are leaked to the cron response including potentially sensitive details like internal IPs, hostnames, and stack traces. | Line 147: `status: \`error: ${err.message}\`` — raw error message from SSH/DB operations exposed in response. |

## MEDIUM

| ID | File:Line | Description | Evidence |
|----|-----------|-------------|----------|
| PRO_B_MED_01 | `src/app/api/v1/chat/route.ts:45-48` | Content-Length header check can be spoofed (set to 0 while sending a large body). The code does double-check with `rawBody.length` on line 52, but the header check on line 46 gives a false sense of security and should be documented as a pre-filter only. | Line 45-48: `const contentLength = parseInt(request.headers.get("content-length") || "0", 10); if (contentLength > MAX_BODY_SIZE)` — client can omit or fake this header. |
| PRO_B_MED_02 | `src/app/api/v1/threads/[id]/messages/route.ts:88-93` | User message insert doesn't check for errors. If the insert fails (e.g., duplicate ID, constraint violation), the code continues to fetch history and proxy to the VPS, resulting in a message being processed but not persisted. | Line 88-93: `await admin.from("api_thread_messages").insert({...})` — return value `{ error }` is not checked. |
| PRO_B_MED_03 | `src/app/api/v1/threads/[id]/messages/route.ts:162-167` | Assistant message insert also doesn't check for errors. If this fails, the user gets a response but it's not saved to the thread history, causing conversation state inconsistency. | Line 162-167: `await admin.from("api_thread_messages").insert({...})` — no error handling. |
| PRO_B_MED_04 | `src/app/api/v1/threads/route.ts:50-51` | POST body parsing uses `request.json().catch(() => null)` but then accesses `(body as any)?.agent` without null check. If body is null (invalid JSON), `agent` defaults to "default" and `metadata` defaults to `{}`, silently creating a thread from malformed input. | Line 50-51: `const body = await request.json().catch(() => null); const agent = (body as any)?.agent || "default";` — null body silently accepted. |
| PRO_B_MED_05 | `src/app/api/mission-control/tasks/[id]/route.ts:58` | PATCH handler uses `request.json()` without body size validation. The guard's `maxBodySize` only checks the Content-Length header (mc-route-guard.ts:71-78), not the actual body. A client can send a huge JSON payload to exhaust memory. | Line 58: `const body = await request.json();` — no actual body size enforcement (compare with tasks POST route which reads as text and checks length). |
| PRO_B_MED_06 | `src/app/api/mission-control/automation-rules/[id]/route.ts:14-20` | PATCH handler doesn't validate `trigger_type` or `action_type` against the whitelist. The POST handler at `automation-rules/route.ts:35-40` validates these fields, but the PATCH update does not, allowing invalid values to be set via update. | Line 16-19: `const allowed = ["name", "trigger_type", ...]; for (const key of allowed) { if (key in body) updates[key] = body[key]; }` — no value validation, only field name whitelisting. |
| PRO_B_MED_07 | `src/app/api/mission-control/tasks/[id]/comments/route.ts:56-61` | Comment `content` has no max-length validation. A user could submit megabytes of text in a single comment. The `mentions` array also has no length or element validation — could contain arbitrary strings or huge arrays. | Line 57-61: `const { content, parent_id, mentions = [] } = body` — no length checks on content or mentions. |
| PRO_B_MED_08 | `src/app/api/v1/agents/[id]/route.ts:18-21` | Agent detail endpoint doesn't filter by `deployed` status. It returns agent info even if the agent is not deployed, leaking internal configuration. The list endpoint at `agents/route.ts:21` correctly filters `.eq("deployed", true)`. | Line 17-21: `admin.from("user_agents").select(...).eq("user_id", ...).eq("agent_id", id).single()` — no `.eq("deployed", true)`. |
| PRO_B_MED_09 | `src/app/api/mission-control/tasks/[id]/reviews/route.ts:59` | Review POST body parsed with `request.json()` — no body size check. The `reviewer` field from request body is used as a fallback but is accepted without validation (line 106), though it's overridden by the profile name lookup. More importantly, `notes` has no max-length validation. | Line 59-63: `const body = await request.json(); const { reviewer, status, notes } = body` — `notes` has no length limit. |
| PRO_B_MED_10 | `src/app/api/mission-control/tasks/queue/route.ts:32-34` | The `.single()` call on the existing in-progress task query will throw an error if there are multiple in-progress tasks for the same agent (which is possible via the bulk-action endpoint). Should use `.limit(1).maybeSingle()` or handle the multi-row case. | Line 28-34: `.eq("column_id", "in_progress").limit(1).single()` — `.single()` throws if 0 or >1 rows. Should use `.maybeSingle()`. |
| PRO_B_MED_11 | `src/app/api/logs/alerts/route.ts:36-41` | POST body parsing `request.json().catch(() => null)` returns null on invalid JSON, but `condition_config` from the destructured null body becomes undefined, which is then stored as `{}` on line 53. The `notification_target` is stored as null without any validation — for a webhook channel this should be a valid URL. | Line 36-55: No validation on `notification_target` format (URL, email, etc.) based on `notification_channel` type. |
| PRO_B_MED_12 | `src/lib/mc-route-guard.ts:70-78` | Body size check only validates Content-Length header, not actual body. Comment on line 69 says "validate AFTER parsing to prevent Content-Length spoofing" but the actual post-parse validation is never implemented — only the header pre-check exists. | Line 69-78: Comment says "actual validation happens after parse" but no post-parse validation code exists. |

## LOW

| ID | File:Line | Description | Evidence |
|----|-----------|-------------|----------|
| PRO_B_LOW_01 | `src/app/api/v1/conversations/route.ts:29-33` | Agent name filter uses `ilike` with `%` wildcards around user input. While `%` and `_` are escaped, this still performs a full table scan on the `agents` table (not user-scoped). The agents query at line 30-33 has no `.eq("user_id", ...)` filter, so it searches ALL agents across all users. | Line 30-33: `admin.from("agents").select("id, name").ilike("name", \`%${escapedAgent}%\`)` — no user_id filter. |
| PRO_B_LOW_02 | `src/app/api/mission-control/tasks/route.ts:7` | `VALID_PRIORITIES` includes `"urgent"` but `src/types/mission-control.ts:30` defines priority as `"low" | "medium" | "high" | "critical"`. The route accepts `urgent` but the type definition says `critical`. This mismatch could cause data integrity issues. | Route line 7: `["low", "medium", "high", "urgent"]` vs type line 30: `"low" | "medium" | "high" | "critical"` |
| PRO_B_LOW_03 | `src/app/api/v1/chat/batch/[id]/route.ts:17-18` | Batch results include the full `results` array in the response. For completed batches with 50 items, each with full response text, this could be a very large payload. No pagination on batch results. | Line 17: `select("id, status, total, completed, failed, results, ...")` — entire results JSON column returned. |
| PRO_B_LOW_04 | `src/app/api/mission-control/tasks/[id]/route.ts:44` | Catch block on GET returns `{ status: 404 }` for any error, not just "not found". Database connection failures, timeouts, etc. all return 404 instead of 500, making debugging difficult. | Line 42-44: `catch { return NextResponse.json({ error: "Task not found" }, { status: 404 }); }` |
| PRO_B_LOW_05 | `src/app/api/v1/audit-log/route.ts:34-35` | The `from` and `to` date filters accept any string from the user and pass it directly to `.gte()` / `.lte()`. Invalid date strings will cause a Supabase query error. Should validate date format. | Line 34-35: `if (from) query = query.gte("created_at", from); if (to) query = query.lte("created_at", to);` — no date format validation. |
| PRO_B_LOW_06 | `src/app/api/mission-control/tasks/[id]/reviews/route.ts:222` | When firing automation rules after approval moves task to "done", the `oldValue` is hardcoded as `"review"` even though the task may have been in any column when approved. | Line 222-226: `processAutomationRules(user.id, "task_status_changed", "done", { taskId, oldValue: "review", newValue: "done" })` — `oldValue` should come from the task's actual previous `column_id`. |
