# AGENT_02 — Starter Dashboard / Backend Developer Review

## CRITICAL

| ID | File:Line | Description | Evidence |
|----|-----------|-------------|----------|
| 59B_CRIT_01 | `src/app/api/chat/stream/route.ts:186-192` | SSE response returns before stream finishes writing. The `return new Response(readable, ...)` at line 186 fires immediately while the async IIFE (lines 133-184) is still running. If the function scope ends (e.g., Next.js edge function timeout, client disconnect), the IIFE's `finally { await writer.close(); }` may not execute, leaving the TransformStream in an indeterminate state and potentially losing the final assistant message. The IIFE has a bare `catch {}` at line 179 that additionally swallows any errors silently. | `(async () => { ... })(); return new Response(readable, ...)` — no await on the IIFE, no guard on function completion |
| 59B_CRIT_02 | `src/app/api/chat/stream/route.ts:81-85, 171-178` | User message inserted into DB before OpenClaw response is confirmed. If the SSE stream fails to start (line 119-121 returns early with 502) or the IIFE crashes mid-stream (line 179 catch), the user message is already persisted but no assistant reply is stored. User sees a "sent" message with no response and no error indication. No orphan cleanup exists. | Line 81-85 insert user message, then IIFE at line 133 runs asynchronously with no connection to the early return path at line 121 |

## HIGH

| ID | File:Line | Description | Evidence |
|----|-----------|-------------|----------|
| 59B_HIGH_01 | `src/app/api/vps/start/route.ts:41-46` | No transition-state guard on VPS start. Only checks `vps.status === "running"`. If the VPS is in `starting`, `restarting`, or `provisioning` state, the route proceeds to call `startVM()` again, potentially triggering multiple concurrent start operations on the Hostinger API. | Only `if (vps.status === "running")` guard; no check for "starting"/"restarting"/"provisioning" |
| 59B_HIGH_02 | `src/app/api/vps/restart/route.ts:41-52` | Sets `status: "running"` immediately after `restartVM()` returns. Hostinger restart takes 1-5 minutes. The user's dashboard shows "running" while the VPS is still restarting. This is a data integrity issue — the DB state contradicts reality. | Line 51-52: `await admin.from("vps_instances").update({ status: "running" })` immediately after `restartVM()` call with no polling or wait |
| 59B_HIGH_03 | `src/app/api/chat/stream/route.ts:119-121` | When OpenClaw returns a non-ok or non-streamable response, the route returns `new Response(JSON.stringify({ error: "Agent unavailable" }), { status: 502 })` as **text/plain** (no `Content-Type` header set), but the client `agent-chat.tsx` expects JSON and checks `contentType.includes("text/event-stream")`. The client receives a 502 with text/plain body and treats it as an SSE parse failure. | Line 186-192 sets headers but line 121 returns before that with no headers override; `agent-chat.tsx` at line 192 checks `contentType.includes("text/event-stream")` |
| 59B_HIGH_04 | `src/app/api/chat/send/route.ts:278-280` | `sessionKey` is constructed as `${user.id}_${agentSlug}_${Date.now()}` for new sessions, but `Date.now()` has millisecond precision. If a user sends two "new session" messages in the same millisecond (trivial under fast network conditions), both get the same `sessionKey`, causing OpenClaw to merge the sessions. The intent of starting a new conversation is violated. | `const sessionKey = new_session ? \`${user.id}_${agentSlug}_${Date.now()}\` : \`${user.id}_${agentSlug}\`` — `Date.now()` is not guaranteed unique |
| 59B_HIGH_05 | `src/app/api/channels/connect/route.ts:178-179` | Catch block for SSH `configureChannel()` returns a generic error with no server-side logging or error propagation. The underlying SSH error (auth failure, invalid token format, network issue) is completely discarded, making debugging channel connection failures impossible. Compare `agents/deploy` which at minimum has `void message` for debug logging. | `catch (err) { return NextResponse.json({ error: "Failed to connect channel." }, { status: 500 }); }` — zero logging, zero detail preservation |

## MEDIUM

| ID | File:Line | Description | Evidence |
|----|-----------|-------------|----------|
| 59B_MED_01 | `src/lib/idempotency.ts:30-48` | `storeIdempotency` uses `.then(() => {}, () => {})` to silently swallow ALL errors including network failures, DB constraint violations, and authentication errors. If the idempotency write fails, subsequent identical requests will be processed as new requests (not idempotent). For non-idempotent operations like key generation, this could result in duplicate keys being created. | `await admin.from("idempotency_cache").upsert(...).then(() => {}, () => {})` — empty rejection handler swallows everything |
| 59B_MED_02 | `src/app/api/chat/stream/route.ts:157-159` | Thinking-tag stripping in the streaming route only handles `<think>` and `<thinking>`. Missing `</think>` (lowercase) closing tag that some models use. Compare `chat/send/route.ts:357-363` which handles 5 additional patterns: `</think>` (lowercase), `<reasoning>`, `</reasoning>`, `<reflection>`, and `<\|think_start\|>...<\|think_end\|>`. Streamed responses may leak thinking content to end users. | `chat/stream` line 157-159: only `<think>` and `<thinking>`; `chat/send` line 357-363: 7 patterns total |
| 59B_MED_03 | `src/app/api/account/password/route.ts:45-62` | Password change flow creates a throwaway Supabase client (`createAnonClient`) with the service role anon key, calls `signInWithPassword` to verify the current password, then calls `signOut`. The `signOut` call (line 62) is not awaited and has no error handling. If it fails, the throwaway session remains active. Additionally, the throwaway client is created via dynamic `import("@supabase/supabase-js")` inside the function rather than using the existing server client. | Line 62: `await verifyClient.auth.signOut()` — no try/catch, no error handling on failure |
| 59B_MED_04 | `src/app/api/account/password/route.ts:46-50` | The throwaway verification client is created with `NEXT_PUBLIC_SUPABASE_ANON_KEY` (the browser-safe anon key). This is the correct key for client-side auth, but for server-to-server password verification via `signInWithPassword`, this should use the service role key for reliability. The anon key is subject to RLS policies which could theoretically interfere. | `createAnonClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!)` — anon key used for server-side auth |
| 59B_MED_05 | `src/app/api/chat/stream/route.ts:40-48` | The `admin` client (Supabase admin) is captured in closure at line 34, but the IIFE that uses it (lines 133-184) is fire-and-forget (not awaited). If the request ends before the IIFE completes, the admin client may have its underlying HTTP connection closed, potentially causing the `chat_messages` insert at line 173-177 to fail silently. | `admin.from("chat_messages").insert(...)` inside unawaited IIFE at line 133 |
| 59B_MED_06 | `src/app/api/vps/gateway-health/route.ts:89-93` | SSH errors return HTTP 200 with `{ active: false, httpOk: false, version: null, pid: null }`. No `_error` flag or HTTP status code distinction between "VPS not running" (line 34-37 also returns 200) and "SSH connection failed". Client cannot distinguish between "not running" and "failed to check". | Both line 34-37 (not running) and line 89-93 (SSH failure) return 200 OK |
| 59B_MED_07 | `src/app/api/chat/conversations/route.ts:43-66` | The `total` in the response (line 72) is set to `conversations.length` (the deduplicated session count), not the original `count` from the DB query. This means `total` represents deduplicated sessions after client-side `range` slicing, not the actual total matching conversations. A client paginating based on `total` would get incorrect offset calculations. | Line 72: `total: conversations.length` — `conversations` is already sliced by `range()` at line 37 and deduplicated at line 52-66 |
| 59B_MED_08 | `src/app/api/account/delete/route.ts:145-156` | Phase 3 storage cleanup uses `Promise.allSettled` but the ticket attachment cleanup (lines 148-156) calls `admin.storage.from("ticket-attachments").list(id)` which returns a Promise, nested inside `ticketIds.map(...)`. These inner Promises are created but never awaited or added to the `allSettled` array. The storage cleanup for ticket attachments is therefore entirely fire-and-forget with no guarantee of completion. | Lines 145-157: only `admin.storage.from("avatars")...` is in the `allSettled` array; ticket attachment cleanup is outside |
| 59B_MED_09 | `src/app/api/audit-log/route.ts:51-58` | Search parameter sanitization uses `replace(/[%_\\]/g, ...)` but the replacement for `\\` is `\\\\` (doubly escaping), while `%` becomes `\\%` and `_` becomes `\\_`. When this `safe` string is interpolated into `.or(\`action.ilike.%${safe}%\`)` at line 56, the `\\` becomes a literal backslash in the LIKE pattern, which is a valid LIKE wildcard (escape character). An attacker could use `\\` to escape the `%` wrapper and widen the search unexpectedly. | `safe.replace(/[%_\\]/g, (c) => c === '\\\\' ? '\\\\\\\\' : c === '%' ? '\\\\%' : '\\_')` — `\\\\` in JS string = `\\` in the actual string passed to LIKE |
| 59B_MED_10 | `src/app/api/chat/send/route.ts:119-121` | Upsert to `chat_conversations` uses `onConflict: "user_id,agent_id"`. If the `user_id,agent_id` combination does not have a unique constraint in the database, this upsert will either fail silently or behave unexpectedly. The error case (lines 136-141) returns 500 but does not log which conflict occurred. | `admin.from("chat_conversations").upsert({ user_id: user.id, agent_id, updated_at: ... }, { onConflict: "user_id,agent_id" })` — depends on DB constraint |

## LOW

| ID | File:Line | Description | Evidence |
|----|-----------|-------------|----------|
| 59B_LOW_01 | `src/app/api/vps/ssl-check/route.ts` | Not reviewed in detail — the previous review (59_BACKEND_DEV H2) noted this uses the regular Supabase client instead of admin. Would need re-examination to verify current state. | N/A — not re-read |
| 59B_LOW_02 | `src/app/api/chat/stream/route.ts:102-104` | The outer `try/catch` at line 102 wraps the fetch and stream initiation, but the actual streaming happens in the detached IIFE (line 133). A timeout abort fires `controller.abort()`, but the IIFE's `reader.read()` is not connected to this controller — the abort only kills the fetch, not the streaming loop. The timeout is therefore ineffective for slow streams. | `controller.abort()` at line 104 only aborts `fetch()` at line 106; `reader.read()` at line 137 has no abort signal |
| 59B_LOW_03 | `src/app/api/account/delete/route.ts:163` | Auth user deletion at line 163 (`admin.auth.admin.deleteUser(userId)`) is called AFTER Phase 1-3 cleanup (lines 81-160). If auth deletion fails (e.g., due toongoing sessions or SSO provider sync delay), the user's DB data is already deleted but they can still log in, creating a ghost account with no data. The rollback at line 165-169 only covers the auth cleanup failure case, not the partial cleanup failure case. | Line 163 comes after all data cleanup; no rollback if data deletion succeeded but auth deletion fails |
| 59B_LOW_04 | `src/app/api/chat/send/route.ts:82` | `agent_id` in the upsert is inserted as a raw string, not validated to exist in the `agents` table. If a user passes a non-existent `agent_id`, the conversation is created for a ghost agent. This is a data integrity issue (orphaned conversations with no valid agent). | Line 82: `agent_id,` — no existence check on the referenced `agent_id` |
| 59B_LOW_05 | `src/app/api/models/change/route.ts:186-188` | When `configureApiKeys` is called on model change, the `email` field passed is `user.email || ""`. If `user.email` is null (possible if the auth user was created via SSO without an email), an empty string is passed to OpenClaw's gateway config. This could cause issues if OpenClaw expects a valid email for certificate provisioning or notifications. | `email: user.email || ""` — empty string used as fallback with no validation |
