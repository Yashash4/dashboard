[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "clawhq"
version = "0.1.0"
description = "ClawHQ Python SDK — managed OpenClaw hosting API"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}

[project.urls]
Homepage = "https://clawhq.tech"
Documentation = "https://app.clawhq.tech/api-access"
